import Resume from './resume';
import Direct from './direct';
import { UploadOptions, UploadHandler } from './base';
import Logger from '../logger';
export * from './base';
export * from './resume';
export default function createUploadManager(options: UploadOptions, handlers: UploadHandler, logger: Logger): Resume | Direct;
